apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "my-nextjs-app.fullname" . }}
spec:
  replicas: 3
  selector:
    matchLabels:
      app: {{ include "my-nextjs-app.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "my-nextjs-app.name" . }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          ports:
            - containerPort: 80